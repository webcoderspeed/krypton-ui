---
title: Input
description: A versatile input component for collecting user data with various configurations and states.
---

<ComponentPreview name="InputPlayground" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn-ui@latest add input
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="input" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { Input } from "@/components/ui/input"
```

```tsx
<Input type="email" placeholder="Email" />
```

## Examples

### Input with Label

<ComponentPreview name="InputWithLabelPlayground" />

### Input with Icon

<ComponentPreview name="InputWithIconPlayground" />

### Search Input

<ComponentPreview name="SearchInputPlayground" />

### Form Input

<ComponentPreview name="FormInputPlayground" />

## API Reference

### Input

The Input component extends the standard HTML input element with enhanced styling and functionality.

#### Props

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `type` | `string` | `"text"` | The input type (text, email, password, etc.) |
| `placeholder` | `string` | - | Placeholder text |
| `value` | `string` | - | Controlled input value |
| `defaultValue` | `string` | - | Default input value |
| `disabled` | `boolean` | `false` | Whether the input is disabled |
| `required` | `boolean` | `false` | Whether the input is required |
| `readOnly` | `boolean` | `false` | Whether the input is read-only |
| `className` | `string` | - | Additional CSS classes |
| `onChange` | `function` | - | Change event handler |
| `onFocus` | `function` | - | Focus event handler |
| `onBlur` | `function` | - | Blur event handler |
| `...props` | `InputHTMLAttributes` | - | All standard input attributes |

## Input Types

### Text Input

```tsx
<Input type="text" placeholder="Enter your name" />
```

### Email Input

```tsx
<Input type="email" placeholder="Enter your email" />
```

### Password Input

```tsx
<Input type="password" placeholder="Enter your password" />
```

### Number Input

```tsx
<Input type="number" placeholder="Enter a number" min="0" max="100" />
```

### Search Input

```tsx
<Input type="search" placeholder="Search..." />
```

### URL Input

```tsx
<Input type="url" placeholder="https://example.com" />
```

### Tel Input

```tsx
<Input type="tel" placeholder="+1 (555) 123-4567" />
```

### Date Input

```tsx
<Input type="date" />
```

### Time Input

```tsx
<Input type="time" />
```

### File Input

```tsx
<Input type="file" accept=".pdf,.doc,.docx" />
```

## States

### Default State

```tsx
<Input placeholder="Default input" />
```

### Disabled State

```tsx
<Input placeholder="Disabled input" disabled />
```

### Read-only State

```tsx
<Input value="Read-only value" readOnly />
```

### Required State

```tsx
<Input placeholder="Required input" required />
```

### With Error State

```tsx
<Input 
  placeholder="Input with error" 
  className="border-red-500 focus-visible:ring-red-500" 
  aria-invalid="true"
/>
```

## Styling

### Custom Styling

```tsx
<Input 
  placeholder="Custom styled input"
  className="border-blue-500 focus-visible:ring-blue-500 bg-blue-50"
/>
```

### Size Variants

```tsx
{/* Small */}
<Input 
  placeholder="Small input" 
  className="h-8 text-sm"
/>

{/* Default */}
<Input placeholder="Default input" />

{/* Large */}
<Input 
  placeholder="Large input" 
  className="h-12 text-lg"
/>
```

### Full Width

```tsx
<Input placeholder="Full width input" className="w-full" />
```

### Fixed Width

```tsx
<Input placeholder="Fixed width" className="w-64" />
```

## Form Integration

### With React Hook Form

```tsx
import { useForm } from "react-hook-form"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { Button } from "@/components/ui/button"

function MyForm() {
  const { register, handleSubmit, formState: { errors } } = useForm()

  const onSubmit = (data) => {
    console.log(data)
  }

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
      <div>
        <Label htmlFor="email">Email</Label>
        <Input
          id="email"
          type="email"
          {...register("email", { required: "Email is required" })}
          className={errors.email ? "border-red-500" : ""}
        />
        {errors.email && (
          <p className="text-red-500 text-sm mt-1">{errors.email.message}</p>
        )}
      </div>
      <Button type="submit">Submit</Button>
    </form>
  )
}
```

### With Controlled State

```tsx
import { useState } from "react"
import { Input } from "@/components/ui/input"

function ControlledInput() {
  const [value, setValue] = useState("")

  return (
    <Input
      value={value}
      onChange={(e) => setValue(e.target.value)}
      placeholder="Controlled input"
    />
  )
}
```

## Input with Icons

### Leading Icon

```tsx
import { Search } from "lucide-react"
import { Input } from "@/components/ui/input"

<div className="relative">
  <Search className="absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400 h-4 w-4" />
  <Input placeholder="Search..." className="pl-10" />
</div>
```

### Trailing Icon

```tsx
import { Eye, EyeOff } from "lucide-react"
import { Input } from "@/components/ui/input"
import { Button } from "@/components/ui/button"

function PasswordInput() {
  const [showPassword, setShowPassword] = useState(false)

  return (
    <div className="relative">
      <Input
        type={showPassword ? "text" : "password"}
        placeholder="Password"
        className="pr-10"
      />
      <Button
        type="button"
        variant="ghost"
        size="sm"
        className="absolute right-0 top-0 h-full px-3 py-2 hover:bg-transparent"
        onClick={() => setShowPassword(!showPassword)}
      >
        {showPassword ? (
          <EyeOff className="h-4 w-4" />
        ) : (
          <Eye className="h-4 w-4" />
        )}
      </Button>
    </div>
  )
}
```

## Validation

### HTML5 Validation

```tsx
<Input
  type="email"
  placeholder="Email"
  required
  pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$"
  title="Please enter a valid email address"
/>
```

### Custom Validation

```tsx
function ValidatedInput() {
  const [value, setValue] = useState("")
  const [error, setError] = useState("")

  const validate = (value) => {
    if (value.length < 3) {
      setError("Must be at least 3 characters")
    } else {
      setError("")
    }
  }

  return (
    <div>
      <Input
        value={value}
        onChange={(e) => {
          setValue(e.target.value)
          validate(e.target.value)
        }}
        placeholder="Enter text"
        className={error ? "border-red-500" : ""}
        aria-invalid={!!error}
        aria-describedby={error ? "error-message" : undefined}
      />
      {error && (
        <p id="error-message" className="text-red-500 text-sm mt-1">
          {error}
        </p>
      )}
    </div>
  )
}
```

## Accessibility

### Keyboard Navigation

- **Tab**: Move focus to the input
- **Enter**: Submit form (if in a form)
- **Escape**: Clear input (custom implementation)

### Screen Reader Support

```tsx
<div>
  <Label htmlFor="username">Username</Label>
  <Input
    id="username"
    type="text"
    placeholder="Enter your username"
    aria-describedby="username-help"
    required
  />
  <p id="username-help" className="text-sm text-gray-600">
    Must be 3-20 characters long
  </p>
</div>
```

### Error Handling

```tsx
<div>
  <Label htmlFor="email">Email</Label>
  <Input
    id="email"
    type="email"
    aria-invalid={hasError}
    aria-describedby={hasError ? "email-error" : "email-help"}
  />
  {hasError ? (
    <p id="email-error" className="text-red-500 text-sm" role="alert">
      Please enter a valid email address
    </p>
  ) : (
    <p id="email-help" className="text-gray-600 text-sm">
      We'll never share your email
    </p>
  )}
</div>
```

## Best Practices

### Labeling

```tsx
// Good: Always provide labels
<div>
  <Label htmlFor="email">Email Address</Label>
  <Input id="email" type="email" />
</div>

// Avoid: Input without label
<Input type="email" placeholder="Email" />
```

### Placeholder Text

```tsx
// Good: Descriptive placeholder
<Input placeholder="Enter your full name" />

// Avoid: Placeholder as label
<Input placeholder="Name" />
```

### Error Messages

```tsx
// Good: Specific error messages
<Input aria-invalid={true} />
<p role="alert">Email must be in format: user@domain.com</p>

// Avoid: Generic error messages
<Input className="error" />
<p>Invalid input</p>
```

### Form Structure

```tsx
// Good: Proper form structure
<form>
  <fieldset>
    <legend>Contact Information</legend>
    <div>
      <Label htmlFor="name">Name</Label>
      <Input id="name" type="text" required />
    </div>
    <div>
      <Label htmlFor="email">Email</Label>
      <Input id="email" type="email" required />
    </div>
  </fieldset>
  <Button type="submit">Submit</Button>
</form>
```

## Common Patterns

### Search Input

```tsx
import { Search, X } from "lucide-react"

function SearchInput() {
  const [query, setQuery] = useState("")

  return (
    <div className="relative">
      <Search className="absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400 h-4 w-4" />
      <Input
        type="search"
        placeholder="Search..."
        value={query}
        onChange={(e) => setQuery(e.target.value)}
        className="pl-10 pr-10"
      />
      {query && (
        <Button
          type="button"
          variant="ghost"
          size="sm"
          className="absolute right-0 top-0 h-full px-3"
          onClick={() => setQuery("")}
        >
          <X className="h-4 w-4" />
        </Button>
      )}
    </div>
  )
}
```

### Input Group

```tsx
function InputGroup() {
  return (
    <div className="flex">
      <span className="inline-flex items-center px-3 rounded-l-md border border-r-0 border-gray-300 bg-gray-50 text-gray-500 text-sm">
        https://
      </span>
      <Input
        type="text"
        placeholder="www.example.com"
        className="rounded-l-none"
      />
    </div>
  )
}
```

### Auto-resize Textarea Alternative

```tsx
// For multiline input, consider using Textarea component
import { Textarea } from "@/components/ui/textarea"

<Textarea placeholder="Enter multiple lines..." />
```

## Troubleshooting

### Common Issues

**Input not focusing:**
- Ensure the input is not disabled
- Check for overlapping elements
- Verify proper event handlers

**Styling not applying:**
- Check className conflicts
- Ensure Tailwind classes are not being purged
- Verify CSS specificity

**Form submission issues:**
- Ensure proper form structure
- Check event handlers
- Verify input names and values

### Performance Considerations

- Use `defaultValue` for uncontrolled inputs when possible
- Debounce onChange handlers for search inputs
- Avoid unnecessary re-renders with React.memo
- Use proper key props in dynamic lists